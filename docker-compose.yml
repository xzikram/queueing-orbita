version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: orbita_postgres
    environment:
      POSTGRES_USER: orbita
      POSTGRES_PASSWORD: orbita_secret
      POSTGRES_DB: orbita_queue
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    container_name: orbita_redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    restart: unless-stopped

volumes:
  postgres_data:
  redis_data:
